def creation(ants, carte, turn):
    """This function will create a ant every 5 turns next to each players anthill.
    
    Parameters
    ----------
    map: the map (dict)
    ant: data concerning the ants (dict)
    turn_count: Counts how many turn has been taken(int)

    Notes
    -----

    Returns
    -------
    map: the map (dict)
    ant: data concerning the ants (dict)

    """
    plevel1 = level[0]
    plevel2 = level[1]
    
    
    Return(map,ant)
